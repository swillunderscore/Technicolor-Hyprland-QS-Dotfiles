# Keybind overrides written by Settings → Hotkeys. Each rebind is a pair:
#   unbind = <original mods>, <original key>
#   bind   = <new mods>, <new key>, <dispatcher>, <arg>
# Sourced last in hyprland.conf so these win. Safe to edit by hand or delete.
